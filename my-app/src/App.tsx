import React, { FC } from "react";
import Box from "./components/Box";

const App: FC = () => {
  return (
    <div>
      <Box color="pink" width={300} height={300} />
    </div>
  );
};

export default App;
